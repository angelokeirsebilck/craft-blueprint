{% for alt in urlAlternatives %}
	<link rel="alternate" hreflang="{{ alt.lang|slice(0, 2) }}" href="{{ alt.url }}">

	{% if alt.lang|slice(0, 2) == 'nl' %}
		<link rel="alternate" hreflang="x-default" href="{{ alt.url }}">
	{% endif %}
{% endfor %}